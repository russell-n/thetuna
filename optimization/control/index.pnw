The Control
===========

The Control code interacts with the user and builds and runs the optimizations.

<<name='imports', echo=False>>=
# this package
from commoncode.index_builder import create_toctree
@

<<name='toctree', echo=False, results='sphinx'>>=
create_toctree()
@

