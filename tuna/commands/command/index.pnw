The Command
===========

<<name='imports', echo=False>>=
# this package
from cameraobscura import create_toctree
@

This is a base for other (not complicated) commands can use as a starting point.

<<name='toctree', echo=False, results='sphinx'>>=
create_toctree()
@

